Select the loot tables to be randomly distributed among chests generated within this structure.

When a `minecraft:chest` is placed as part of this structure, the generator will automatically replace its data with one of the selected loot tables.
If multiple tables are selected, they will be distributed with equal probability.
