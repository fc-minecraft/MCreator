Какие значения может принимать это свойство?

*   Если это **число** (Integer): напиши диапазон, например `0, 15` (от 0 до 15, как редстоун).
*   Если это **список** (Enum): напиши слова через запятую, например `north, south, east, west`.
